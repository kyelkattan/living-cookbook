{
  "name": "living-cookbook",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"nodemon --legacy-watch server/index.js\" \"npm --prefix client run dev\"",
    "server": "node server/index.js",
    "setup": "npm install && cd client && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.10"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
